# GraphDB Automatization

Проект для автоматизации работы с графовой базой данных Neo4j. Приложение позволяет загружать данные из Google Sheets или Excel файлов, анализировать их и автоматически генерировать графы и изображения на основе табличных данных.

## 🚀 Функции

- **Загрузка данных**: Поддержка Google Sheets и Excel файлов
- **Анализ таблиц**: Автоматический анализ дат и сумм
- **Генерация изображений**: Создание визуализаций на основе данных
- **Интеграция с Neo4j**: Автоматическое создание узлов и связей в графовой БД
- **Графический интерфейс**: Удобный UI на базе Tkinter для управления процессом

## 📋 Требования

- Python 3.13+
- Аккаунт Google Cloud с включенным Google Sheets API
- Установленный Neo4j сервер

## 🛠 Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/your-username/GraphDB_Automatization.git
   cd GraphDB_Automatization
   ```

2. **Установите зависимости:**
   ```bash
   # Используя pip
   pip install .
   
   # Или используя uv (рекомендуется)
   uv sync
   ```

3. **Настройте Google Sheets API:**
   - Создайте проект в Google Cloud Console
   - Включите Google Sheets API
   - Создайте сервисный аккаунт и скачайте credentials.json
   - Поместите файл в `src/config/googlesheets_credentials.json`

4. **Настройте Neo4j:**
   - Установите и запустите Neo4j сервер
   - Настройте подключение в конфигурационных файлах

## 🚀 Использование

1. **Запуск приложения:**
   ```bash
   python main.py
   ```

2. **Загрузка данных:**
   - Введите Sheet ID для Google Sheets
   - Или выберите Excel файл через интерфейс

3. **Анализ данных:**
   - Выберите период для анализа
   - Просмотрите суммы и статистику

4. **Генерация графа:**
   - Автоматически создаются узлы в Neo4j
   - Генерируются изображения визуализаций

## 📁 Структура проекта

```
GraphDB_Automatization/
├── main.py                 # Точка входа в приложение
├── pyproject.toml          # Конфигурация проекта
├── README.md              # Документация
├── data/                  # Тестовые данные
├── images/                # Сгенерированные изображения
└── src/
    ├── config/            # Конфигурационные файлы
    ├── controllers/       # Контроллеры приложения
    ├── services/          # Бизнес-логика
    │   ├── graph_service.py      # Работа с Neo4j
    │   ├── image_generator.py    # Генерация изображений
    │   ├── table_analyzer.py     # Анализ таблиц
    │   └── table_reader.py       # Чтение данных
    └── ui/                # Графический интерфейс
```

## 🔧 Конфигурация

- **Google Sheets**: Разместите credentials в `src/config/googlesheets_credentials.json`
- **Neo4j**: Настройте подключение в соответствующих сервисах
- **Переменные окружения**: Используйте `.env` файл для чувствительных данных

## 📊 Зависимости

- `google-auth` - Аутентификация Google
- `gspread` - Работа с Google Sheets
- `neo4j` - Драйвер Neo4j
- `openpyxl` - Работа с Excel
- `pandas` - Анализ данных
- `python-dotenv` - Переменные окружения
- `requests` - HTTP запросы